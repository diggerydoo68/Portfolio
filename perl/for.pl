#!/usr/bin/perl
@array_list =('these','are', 'some', 'values');
foreach $line(@array_list){
        print "$line";
    }
print "$line";

# Does not print anything unless print is outside the for loop